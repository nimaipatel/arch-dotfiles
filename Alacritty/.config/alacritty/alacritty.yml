key_bindings:
  - { key: Return,   mods: Control|Shift, action: SpawnNewInstance }

cursor:
  style:
    blinking: Always
  thickness: 0.1
  vi_mode_style: Block

window:
  opacity: 1.0

import:
  - ~/.config/alacritty/alacritty-base16.yml
