#!/bin/sh

if xrdb -query | grep -q '\*\.ligatures:\s0' ; then
	echo '*.ligatures: 1' | xrdb -merge
else
	echo '*.ligatures: 0' | xrdb -merge
fi

# send USR1 signal to all running st instances
pidof st | xargs kill -s USR1
